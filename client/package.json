{
  "name": "you-and-i",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-pose": "^4.0.9",
    "react-pose-text": "^3.1.0",
    "react-scripts": "3.2.0",
    "react-scroll-parallax": "^2.2.0",
    "three": "^0.109.0",
    "three-obj-loader": "^1.1.3"
  },
  "proxy": "http://localhost:5000/",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "rm -R ../public && mkdir ../public && mv build/* ../public",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "postcss-normalize": "^8.0.1"
  }
}
